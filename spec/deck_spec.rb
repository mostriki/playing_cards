require 'rspec'
require 'pry'
require 'deck'

describe ('Deck') do


  describe ('') do
    it ('returns a set of 52 shuffled cards') do
      expect().to(eq()
    end
  end

  describe ('') do
    it ('returns an array without a card') do
      expect().to(eq()
    end
  end

  describe ('') do
    it ('returns a count of 52 playing cards in a deck') do
      expect().to(eq()
    end
  end

end